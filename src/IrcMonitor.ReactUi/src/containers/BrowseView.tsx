import { AppContentWrapper } from "framework/AppContentWrapper";
import React from "react";

export const BrowseView: React.FC = () => {
  return (
    <AppContentWrapper title="Browse">
      <p>Browse discussion in a channel</p>
    </AppContentWrapper>
  );
};
